#include "main.h"

/**
 * mul - Multiplies two integers
 * @x: The first digit to multiply
 * @y: The second digit to multiply with the first digit
 *
 * Return: The result at multiplication between x and y
 */

int mul(int x, int y)

{

	return (x * y);
}
