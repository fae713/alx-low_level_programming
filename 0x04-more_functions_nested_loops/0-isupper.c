#include "main.h"

/**
 * _isupper - Checks if a letter is uppercase character.
 * @x: The number to be checked
 *
 * Return: 1 for upper letter or 0 for anything else
 */

int _iaupper(int x)

{

	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}
