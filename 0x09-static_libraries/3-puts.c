#include "main.h"
#include<stdio.h>

/**
 * main - Entry point
 * _puts: write a line or string to the output
 *
 * Return: Always 0 (Success)
 */
int main(void)
void _puts(char *str)
{
	puts("\"Programming is like building a multilingual puzzle");

	return (0);
}
